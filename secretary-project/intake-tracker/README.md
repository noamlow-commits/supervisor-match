# מעקב פניות והרשמה — תוכניות הדגל (בית הספר הדיאלוגי)

תת-פרויקט בתוך `secretary-project/`. מערכת web למעקב אחר פניות להרשמה לשתי תוכניות הדגל, מבוססת Google Sheets כ-backend, בקו האדריכלות הקיים (Apps Script + Sheets, חינם, בלי שרת).

> **סטטוס:** Phase 1 — **פרוס ועובד** תחת חשבון `merkazrotenbergkh@gmail.com` (2026-06-17). 62 רשומות מיובאות נקי. הקוד ב-`apps-script/`, הוראות ב-[INSTALL.md](INSTALL.md). הקובץ החי **לא נגענו בו**. ראו "עדכון מצב" בתחתית.

### קבצים (apps-script/)
| קובץ | תפקיד |
|---|---|
| `config.gs` | מזהים, פרמטרים, סכמת `פניות`, מילון נרדפות-כותרות, שלבי משפך — מקור-אמת יחיד |
| `lib.gs` | גישה ל-App DB, נרמול, חישוב שלב/פעולה/תזכורת, מפתח זהות ל-upsert |
| `setup.gs` | הקמה חד-פעמית של ה-App DB (לשוניות, ולידציה, עיצוב מותנה, תפריט) |
| `importer.gs` | סנכרון חד-כיווני קובץ-חי→App DB; מונה לשוניות (כל אחת=תוכנית), זיהוי כותרות גמיש |
| `Code.gs` | שרת ה-Web App (board / update / create / dashboard / import / send-material) |
| `index.html` | ממשק RTL: קנבן לפי שלב + דשבורד + כרטיס עריכה; פלטה רגועה (slate/teal) |

**הרצה:** ראו [INSTALL.md](INSTALL.md) — מדביקים תחת חשבון רוטנברג, `setup()` → `runImport()` → Deploy as Web App. ~15 דק'.

---

## 1. מטרה

לעקוב ולעדכן את משפך הפניות לשתי תוכניות הדגל — מרגע הפנייה ועד הרשמה משלמת — דרך ממשק נוח, עם הזנת נתונים חצי-אוטומטית (המזכירה מסמנת עובדות אטומיות; השלב/הפעולה-הבאה/התזכורות מחושבים לבד).

**התוכניות:** **כל לשונית בקובץ החי = תוכנית בפני עצמה** (נועם, 2026-06-17). אין מספר קבוע — ה-importer יעבור על כל הלשוניות דינמית, וכל לשונית תהפוך לערך ב-`תוכנית`. בין התוכניות שזוהו: *כוח הצמצום* (תעודה חד-שנתית, ראיון עם הרב ד"ר רונן בן-דוד), *תעודה תשפ"ז*, *מחזור כ"א תשפ"ו*, ועוד.

**משתמשים:** טל (מזכירה) · מיכל (מנהלת המכון) · אנשי צוות ניהול כולל נועם. **הפונים לא מקבלים גישה לקובץ/אפליקציה.**

**חשבון מריץ:** חשבון ה-Gmail של **מכון רוטנברג** (החשבון שממנו מקורו של הקובץ) — תחתיו ייכתב ויורץ ה-Apps Script וממנו יישלחו ההודעות. **לא** החשבון של נועם.

---

## 2. החלטות יסוד (נועם, 2026-06-17)

1. **היעד הסופי = ממשק web**, ובסוף עובדים **רק דרכו**.
2. **אסור לפגוע בקובץ החי** — הוא מקור-האמת היום. כל בנייה היא **מקבילה**, עד שהמערכת מלאה.
3. כי הבנייה מקבילה — **חובה מסלול מיגרציה/איחוד מפורש** ל-cutover (מעבר לעבודה דרך האפליקציה בלבד).
4. כרגע **אין גישה ל-CRM** של המכון — רק לגיליון. הזרם חד-כיווני; הגיליון/האפליקציה הם מקור-האמת למשפך, ה-CRM מחזיק את רשומת הלקוח הרשמית.
5. **כל לשונית בקובץ = תוכנית.** ה-importer מונה לשוניות דינמית; כל לשונית → ערך ב-`תוכנית`. מתרחב ל-N תוכניות בלי שינוי קוד.
6. **משתמשים:** טל (מזכירה), מיכל (מנהלת), צוות ניהול כולל נועם. הפונים — ללא גישה.
7. **חשבון מריץ = מכון רוטנברג** (מקור הקובץ). ה-Apps Script ייכתב/יורץ תחתיו; ההודעות יישלחו ממנו.

---

## 3. מצב הקובץ החי (מה שמצאנו)

הטאב החי הוא CRM-בתוך-שיטס: ~8 טבלאות שהודבקו אנכית באותו טאב.

| # | טבלה | תוכן |
|---|------|------|
| 1 | אינטייק ראשי (~31 עמודות) | המשפך המלא: שיחה→מסמכים→ראיון→תשלומים→תוצאה |
| 2 | רשימת שמות + **קוד לקוח** | קודי CRM (לדוגמה 210216) |
| 3 | קישורי Cardcom | דמי הרשמה **300**, מקדמה **1200** |
| 4 | **תעודה תשפ"ז** (~15 שורות) | אותה סכמה, התוכנית השנייה |
| 5 | תבנית הודעה | "תודה שהתעניינת... כח הצמצום" |
| 6 | ממתינים לראיון עם הרב רונן | יעל רוס, רז לוי... |
| 7 | מחזור כ"א תשפ"ו (נרשמו) | שילמו 4900 |
| 8 | אנשי קשר/מרצים | דן טיומקין, סימון שירן... |

**עמודות המשפך (מאוחדות):** ערוץ פנייה · נשלחה הודעה · שוחחנו? · [סף קבלה: תואר שני / ניסיון שנתיים / עבר במכון] · מסמכי תעודות · המלצות · טופס הרשמה · טפסים חסרים · מצב ראיון · [תשלומים: דמי ראיון / דמי הרשמה 300 / מקדמה 1200 / שכ"ל 4900] · טופס הנחה · אחוז הנחה · מכתב תוצאה · סטטוס · **קוד לקוח / הערות בCRM** · סיבה שאין עניין.

**קריאת התרשים (כתב היד):**
```
פונים/נרשמים באתר → נכנסת פנייה → חומר נכתב ונשלח → "תהליך פנייה"
   → (לכל המתעניינים) → הרשמה לתוכנית
```
- העיגולים "תזכיר/תאריך" באמצע = **מנגנון תזכורות/מעקב** ללידים מתקררים.
- הטבלה בתחתית (פרטים · הרשמה+קבלה+תאריך · תשלום+קבלה+תאריך · סטטוס) = כל אבן-דרך עם תאריך וקבלה.
- הפס השמאלי (300 / 1800 / 80 ש"ח / "סוג לקוח" / CRM) = סיעור תמחור, שיקול משני.

---

## 4. ארכיטקטורה — שלושה רכיבים

```
┌─────────────────────┐     importer (חד-כיווני,        ┌──────────────────────┐
│  הקובץ החי           │ ──  לקריאה בלבד, לפי דרישה)  ──▶ │  App DB (גיליון חדש)  │
│  (לא נוגעים בו)       │                                  │  טבלת "פניות" מנורמלת │
│  המזכירה ממלאת       │                                  └──────────┬───────────┘
└─────────────────────┘                                              │ read/write
                                                          ┌──────────▼───────────┐
                                                          │  Web App (Apps Script)│
                                                          │  Kanban + טופס עדכון   │
                                                          │  דשבורד + תזכורות     │
                                                          └──────────────────────┘
```

1. **הקובץ החי** — נשאר כפי שהוא, מקור-אמת בתקופת המעבר. **לא כותבים אליו.**
2. **App DB** — Google Sheet **חדש ונפרד**, סכמה מנורמלת (טבלה אחת `פניות` + טאבי lookup). ה-backend של האפליקציה.
3. **Web App** — Apps Script Web App (כמו `admin.html` ב-supervisor-match) שקורא/כותב ל-App DB בלבד.

**ה-importer** הוא לב הבטיחות: סקריפט שקורא את הקובץ החי, **מונה את כל הלשוניות (כל לשונית = תוכנית)**, מפענח כל אחת לפי שורת-הכותרת שלה, ו-upsert לתוך App DB לפי `קוד לקוח`/שם+טלפון, עם `תוכנית` = שם הלשונית. רץ לפי דרישה — **קריאה בלבד מהקובץ החי**, אפס כתיבה אליו. תוכניות חדשות (לשוניות חדשות) נקלטות אוטומטית.

---

## 5. אסטרטגיית המעבר (3 שלבים) — מבטיחה אי-פגיעה + איחוד ברור

### Phase 1 — מקביל, קריאה + הרחבה (לא נוגעים בקובץ)
- בונים App DB + Web App.
- ה-importer מסנכרן קובץ-חי → App DB (חד-כיווני).
- המזכירה **ממשיכה למלא בקובץ החי**. האפליקציה נותנת לה **תצוגה** יפה: משפך, דשבורד, לידים תקועים, תזכורות.
- כתיבה באפליקציה בשלב זה = רק **שדות הרחבה שהקובץ לא מכיר** (snooze תזכורת, הערת פעולה-הבאה), ממופתחים לפי קוד לקוח — בלי התנגשות עם ה-import הבא.
- **בלי סיכון:** אם משהו לא עובד, פשוט ממשיכים בקובץ.

### Phase 2 — כתיבה מלאה ב-App DB (עדיין מקביל)
- האפליקציה הופכת ל-read-write מלא על App DB: כל עדכוני המשפך נעשים באפליקציה.
- ה-importer ממשיך לרוץ למקרה שעדיין נכנס משהו בקובץ הישן, עם זיהוי-קונפליקטים (אם שתי הגרסאות שונות — מסמנים לסקירה ידנית).
- בודקים שהאפליקציה מחזיקה הכול נכון מול הקובץ.

### Phase 3 — Cutover (איחוד סופי)
- מיגרציה אחרונה: קובץ-חי → App DB כ-master.
- מכבים את ה-importer. הקובץ הישן הופך לארכיון/גיבוי (read-only).
- מכאן והלאה **רק האפליקציה**. תיעוד ה-cutover נשמר כאן.

---

## 6. סכמת App DB — טבלת `פניות` מנורמלת

עמודה אחת לכל עובדה אטומית. אבני-דרך = ✓ + תאריך + מס' קבלה.

**א. זיהוי**
`id` · `תאריך פנייה` · `שם` · `תוכנית` (כוח הצמצום / תלת-שנתית) · `מחזור` (תשפ"ז / כא תשפ"ו...) · `ערוץ פנייה` (אתר/טלפון/וואטסאפ/המלצה) · `קוד לקוח (CRM)`

**ב. קשר**
`טלפון` · `דואל` · `מגורים` · `עיסוק`

**ג. סף קבלה** (checkbox)
`תואר שני` · `ניסיון שנתיים` · `עבר במכון`

**ד. תהליך** (✓ + תאריך)
`נשלח חומר` · `שוחחנו` · `טופס הרשמה` · `מסמכי תעודות` · `המלצות` · `טפסים חסרים` (טקסט) · `מצב ראיון` (לא נקבע/נקבע/בוצע/התקבל/נדחה) + `תאריך ראיון`

**ה. תשלומים** (לכל אחד: ✓ + סכום + תאריך + מס' קבלה)
`דמי ראיון` · `דמי הרשמה` (300) · `מקדמה` (1200) · `שכ"ל` (4900) · `טופס הנחה` + `אחוז הנחה`

**ו. תוצאה**
`מכתב תוצאה` (נשלח/לא) · `סטטוס` · `סיבה שאין עניין` · `נרשם בCRM` (✓)

**ז. מחושב (Apps Script — לא להקלדה)**
`שלב במשפך` · `ימים מאז עדכון` · `פעולה הבאה` · `תאריך עדכון` (אוטו-חותמת)

**טאבי lookup:** `סטטוסים` · `שלבי משפך` · `סוגי תשלום` · `תבניות הודעה` · `הרחבות אפליקציה` (snooze/הערות).

---

## 7. לוגיקת המשפך (החצי-אוטומציה)

המזכירה מסמנת רק עובדות אטומיות. נגזר אוטומטית:

| שלב במשפך (נגזר מהאבן-דרך הגבוהה ביותר) | פעולה הבאה מוצעת |
|---|---|
| 0 · פנייה חדשה | לשלוח חומר |
| 1 · נשלח חומר | ליצור קשר / לשוחח |
| 2 · שוחחנו | לאסוף מסמכים + המלצות |
| 3 · מסמכים מלאים | לקבוע ראיון |
| 4 · ראיון נקבע | לקיים ראיון |
| 5 · התקבל | לגבות דמי הרשמה (300) |
| 6 · שילם הרשמה | מקדמה (1200) |
| 7 · שילם מקדמה | שכ"ל (4900) |
| 8 · נרשם (שילם שכ"ל) | לוודא רישום ב-CRM |
| ✗ לא מעוניין / נדחה | (סגור — שמור סיבה) |

**תזכורות (ה"תזכיר" מהתרשים):** ליד ללא עדכון > 14 יום (ניתן לכוונון) → נכנס לרשימת "צריך תזכורת" בדשבורד, אלא אם בוצע snooze.

---

## 8. אינטייק חצי-אוטומטי (אופציונלי, Phase 2+)

Google Form (קישור באתר) → פנייה חדשה מוסיפה שורה לבד ל-App DB בשלב "פנייה חדשה" + שולחת אישור אוטומטי עם תבנית ההודעה. ממש הזרם העליון בתרשים ("פונים באתר → חומר נשלח"). המזכירה רק מעשירה.

---

## 9. פער ה-CRM

אין API כרגע → `קוד לקוח` + `נרשם בCRM ✓` + תצוגה מסוננת "טרם נרשמו בCRM" כצ'ק-ליסט סנכרון ידני. תפר נקי: כשתינתן גישה, פונקציית סנכרון תמפה `קוד לקוח` ↔ שורה.

---

## 10. תוכנית ביצוע — מה נבנה ב-Phase 1

1. ליצור את **App DB** (Google Sheet חדש) עם הסכמה מסעיף 6 + טאבי lookup + data-validation + עיצוב מותנה.
2. לכתוב `importer.gs` — קורא את הקובץ החי, מפענח את 8 הטבלאות, ו-upsert ל-App DB.
3. לכתוב את ה-Web App (`Code.gs` + `index.html`): לוח Kanban של פניות לפי שלב, לחיצה→כרטיס עדכון, דשבורד (משפך לכל תוכנית + תשלומים פתוחים + לידים תקועים).
4. אוטומציות: אוטו-חותמת תאריך, חישוב שלב/פעולה-הבאה, דגל תזכורות, כפתור "שלח חומר" (Gmail).
5. בדיקה מול הקובץ החי (read-only) — לוודא שהמספרים תואמים.

**נסגר (2026-06-17):** תוכניות = כל לשונית · משתמשים = טל/מיכל/ניהול · Gmail = חשבון מכון רוטנברג.

**פתוח אחד לפני Phase 1 — גישה לחשבון רוטנברג:**
מאחר שהאפליקציה רצה תחת חשבון המכון, יש שתי דרכי בנייה:
- **(א) מקצה-לקצה** — נועם נותן לי גישה זמנית לחשבון רוטנברג, ואני מקים הכול (App DB + Apps Script + deploy) שם.
- **(ב) מסירה** — אני כותב את כל הקוד והסכמה כאן בריפו, ונועם/טל מדביקים ומפעילים תחת החשבון (כמו ש-supervisor-match הוקם). אני מספק הוראות התקנה צעד-אחר-צעד.
ברירת מחדל מומלצת: **(ב)** — בלי שיתוף סיסמאות, ותואם לאופן שבו supervisor-match כבר עובד.

---

## עדכון מצב — 2026-06-17 (אבחון, תיקונים, פריסה)

### מצב נוכחי
- האפליקציה **פרוסה ועובדת** כ-Web App תחת `merkazrotenbergkh@gmail.com` (Execute as Me, Access: כל מי שיש לו חשבון Google + קישור פרטי).
- **62 רשומות** מיובאות נקי מהקובץ החי (לעומת 99 עם זבל לפני התיקונים).
- 7 קבצי Apps Script: `config / lib / setup / importer / Code / index.html / diag`.

### מפת הקובץ החי האמיתית (מ-`diagnose()`) — 4 לשוניות
| # | לשונית | תפקיד | שורות | recordType |
|---|--------|-------|------|-----------|
| 0 | נרשמים הדיאלוגי מחזור ג תשפז | משפך — תלת-שנתית | 53 (כותרת בשורה 2) | פנייה |
| 1 | נרשמים תעודה מחזור כב תשפז | משפך — תעודה/כח הצמצום | 55 (יש קוד לקוח; "סטטוס" ×2) | פנייה |
| 2 | נרשמים תוכנית אח"ד תשפז | רשומים ששילמו | 3 (סכמה שונה, 9 עמ') | פנייה |
| 3 | אנשי חינוך ליצירת קבוצה | **רשימת אנשי-קשר** | 26 | איש-קשר |

### בעיות שזוהו בקובץ (למה הוא לא בנוי למשפך)
- סכמות לא-עקביות בין הלשוניות (31/30/9/8 עמ').
- קודי CRM דחוסים בעמודה הלא-נכונה בלשונית 0 ("עבר במכון": `קוד לקוח: 210193`).
- תשלומים מקודדים כסכום (`300`) ולא כ-✓.
- "סטטוס"/"תהליך" טקסט חופשי לא-אחיד.
- תבניות-הודעה ורשימות-קשר מעורבבות בתוך טבלאות הפניות.

### תיקונים שבוצעו (סבב 2)
1. **סינון זבל** — שורה = רשומה רק אם יש טלפון/מייל/קוד לקוח (מנקה שורות-תבנית).
2. **תשלומים** — `toBool_` מזהה ערך מספרי כ"שולם" + שמירת הסכום בשדה הסכום.
3. **חילוץ קוד CRM** שדחוס בעמודות אחרות (חיפוש `2XXXXX` / "קוד לקוח:").
4. **"סטטוס" — מופע אחרון** בלשונית התעודה (הסטטוס האמיתי, לא ההערה).
5. **מפתח זהות לפי תוכנית** — אדם בשתי תוכניות = שתי רשומות (החלטת נועם).
6. **`recordType`** (פנייה / איש-קשר) — לשונית 3 מיובאת כרשימת קשר נפרדת; המשפך והדשבורד מסננים אותה החוצה; לשונית "אנשי קשר" באפליקציה מציגה אותה.

### החלטות (נועם, 2026-06-17)
- **אנשי חינוך** = רשימה נפרדת (לא במשפך). ✅
- **אדם בשתי תוכניות** = שני כרטיסים. ✅
- **כיוון אסטרטגי** = בעתיד **cutover**: אחרי שטל (מזכירה) תעבור על הממשק ותעיר — האפליקציה תהפוך למקור-האמת לשינויים. **מעצבים מעכשיו כאילו האפליקציה היא המרכז** (טבלה מנורמלת אחת + דיסקרימינטור recordType + lookups).

### TODO / פתוח
- ⏳ **טל תעבור על הממשק ותעיר** → אז מחליטים על cutover.
- 🔧 **APP_DB_ID → Script Properties** — כרגע ה-id יושב ב-config.gs, וכל הדבקה מחדש של config מוחקת אותו (קרה פעם אחת). לעבור לאחסון ב-PropertiesService כדי שעדכוני config עתידיים לא ימחקו. (סבב הבא)
- אפשרי בהמשך: צמצום טופס העריכה (50 שדות) למה שטל באמת ממלאת; כפתור "שלח חומר" לתבנית התלת-שנתית; ניקוי פורמט טלפון.
- App DB ישן (יתום מהריצה הראשונה) — אפשר למחוק מ-Drive.

---

## עדכון מצב — 2026-06-17 (סבב הערות טל #1)

טל עברה על הממשק ונתנה 4 הערות. כולן טופלו ואומתו על ידה.

| # | הערה | פתרון |
|---|------|-------|
| 1 | טקסטים שהיא שולחת לפונים בכל תוכנית — להנגיש באפליקציה | `templates.gs` → `seedTemplatesFromLiveFile()` מחלץ אוטומטית את שורות-הטקסט שאינן רשומות-פנייה מכל לשונית → לשונית **"תבניות"** ב-App DB (שורה לתוכנית). בכרטיס: אזור **"תבנית הודעה — [תוכנית]"** + **📋 העתק** (עם `{{שם}}` ממולא); **✉ שלח חומר** שולח את תבנית התוכנית הנכונה. טל יכולה לערוך בלשונית "תבניות". |
| 2 | צבע שונה לכל תוכנית | פס צבע בצד הכרטיס + נקודה + **מקרא** בסרגל. צבע **לפי סדר התוכנית** מפלטה מקסימלית-שונה (כחול/כתום/ירוק/אדום) — לא hash (שנתן גוונים קרובים). |
| 3 | הרשימה נגללת אנכית יותר מדי | הלוח קיבל גובה קבוע (גובה המסך); כל **עמודה נגללת פנימית**; כרטיסים קומפקטיים. |
| 4 | הקצוות נקטעים במסך | ריווח-קצה אחרי העמודה האחרונה + רוחב responsive (`@media`) + שבירת-טקסט. |

**+ תיקון UX:** רשימת אנשי-הקשר ("אנשי חינוך…") הוסרה מהסינן/מקרא/צבעים של הלוח — מופיעה רק בלשונית "אנשי קשר". (בחירתה בסינן הציגה לוח ריק.)

**קבצים שהשתנו:** `Code.gs` (templates בלוח, שליחה לפי תוכנית, `readTemplates_`), `index.html` (צבעים/גלילה/קצוות/תבניות/סינן-משפך-בלבד), `templates.gs` (חדש).

### ⚠️ Gotcha חשוב לפריסה (תוקן בהבנה)
האפליקציה מוגשת מ-**Apps Script** (HtmlService), **לא** מ-GitHub Pages. לכן כתובת ה-`/exec` מגישה תמיד את **הגרסה הפרוסה** — **כל** שינוי, כולל `index.html`, דורש **redeploy (Manage deployments → New version)**. הדבקה + Ctrl+S לבדם לא מספיקים. (שונה ממאגר-המדריכים שמתפרסם אוטומטית דרך Pages.)

---

## עדכון מצב — 2026-06-18 (סבב הערות טל #2: תשלומים)

טל צריכה להזין באפליקציה מי שילם ומי לא. **התשלום מתבצע דרך Cardcom, המעוגן ב-CRM העצמאי של המכון — אין לנו גישה אליו**, לכן טל בודקת ידנית בקארדקום ומסמנת באפליקציה.

**היכולת כבר קיימת:** כרטיס → קטע "תשלומים" → תיבות `דמי ראיון / דמי הרשמה / מקדמה / שכ"ל`, כל אחת עם סכום/תאריך/קבלה. הדשבורד מציג "תשלומים פתוחים".

**נוסף (index.html בלבד), פרוס:**
- **תווית תשלום על הכרטיס** — ירוקה "💳 הרשמה+מקדמה" (מה ששולם); כתומה "💳 ממתין לתשלום" (התקבל אך טרם שילם דמי הרשמה).
- **סינון "ממתין לתשלום"** בסרגל — מציג רק את מי שצריך לבדוק/לגבות.
- הסימון המפורט הקיים נשאר.

**זרימת עבודה לטל:** סינון "ממתין לתשלום" → בדיקה בקארדקום → פתיחת כרטיס → סימון בקטע "תשלומים" → שמירה (התווית נהפכת לירוקה, השלב מתעדכן).

**⏳ פתוח — תשובת טל:** רמת פירוט — מפורט (4 סוגים + סכום/תאריך/קבלה) מול פשוט ("שולם/לא שולם" לשלב). שאלה נשלחה בוואטסאפ; נתאים לפי תשובתה.

**Git:** commit `6a6a55d` (כולל סבב #1: תבניות/צבעים/layout + סבב #2: תשלומים), נדחף ל-main.

### TODO פתוחים
- ⏳ תשובות טל: רמת-פירוט התשלומים; האם התווית+הסינון עוזרים.
- 🔧 העברת `APP_DB_ID` ל-Script Properties (שלא יימחק בהדבקת config).

---

## עדכון מצב — 2026-06-21 (v2: משפכים לפי תוכנית + מבט רוחבי)

מבוסס על ההודעה הקולית של טל (תומללה) + הערות נועם. תכנון מלא: [PLAN-v2-per-program-funnels.md](PLAN-v2-per-program-funnels.md). **קוד מומש ונבדק תחבירית — ממתין לפריסה תחת חשבון המכון.**

### מה השתנה
- **משפך לכל תוכנית** (במקום גלובלי): הדיאלוגי (7 שלבים) / תעודה=כוח-הצמצום (6) / אח"ד (2). שם-לשונית ארוך מנורמל אוטומטית ל-3 תוויות נקיות (`canonicalProgram_`). השלב נגזר מהמשפך הנכון לפי התוכנית.
- **עוגן הרשמה לכל תוכנית** (`PROGRAM_REG_ANCHOR`): הדיאלוגי=מקדמה · תעודה=דמי הרשמה · אח"ד=תשלום מלא → דגל `registered`.
- **צ'ק-ליסט מסמכים** (קבוצת "מסמכים", checkbox לכל אחד, לא מעכב): תעודות / טופס ניסיון / קו"ח / סיפור אישי / פספורט(לא חובה) / המלצה 1 / המלצה 2.
- **שער רך בהדיאלוגי**: ריאיון ללא דמי הרשמה → אזהרה בולטת (`gateWarn`), לא חסימה.
- **שדה תוכנית בכרטיס** = רשימת 3 התוכניות הפעילות + "➕ חדשה".
- **מחיקת פנייה** (`apiDelete` + כפתור 🗑).
- **מבט רוחבי חדש** (לשונית "טבלה רוחבית"): טבלת-על צבעונית — שורה=אדם, עמודות=קשר/מסמכים/דמי-הרשמה/ריאיון/התקבל/נרשם/ימים/תזכורת; ירוק=בוצע, כתום=ממתין, אדום=חסר/תקוע. סינון לפי תוכנית + "צריך טיפול בלבד". זה המסך שמחליף את האקסל.
- ניסוח "נשלח חומר" → תווית "נוצר קשר ונשלח חומר" (תצוגה בלבד, העמודה בגיליון נשארה).

### קבצים שהשתנו
`config.gs` (תוכניות/צבעים/עוגנים/canonicalProgram_/3 משפכים/שדות מסמכים) · `lib.gs` (computeRow_ תלוי-תוכנית, gateWarn_, regAnchorPaid_, ensureColumns_, recordKey קנוני) · `importer.gs` (program קנוני + ensureColumns_) · `Code.gs` (apiBoard funnels/colors, apiDelete, תבניות קנוניות) · `setup.gs` (תיקון הפניות ל-FUNNEL שהוסר) · `index.html` (לשונית טבלה רוחבית, לוח לפי-תוכנית, כרטיס: שער/מחיקה/מסמכים/תוויות).

### צעדי פריסה (תחת חשבון המכון `merkazrotenbergkh@gmail.com`)
1. **הדבק כל קובץ .gs** המעודכן ל-Code.gs/הקבצים המתאימים בעורך Apps Script, שמור.
2. **אל תריץ `setup()`** — הוא מנקה את נתוני הפניות! המיגרציה אוטומטית: `ensureColumns_()` מוסיף את עמודות-המסמכים בעצמו בריצת `apiBoard`/`runImport` הבאה.
3. **Redeploy:** Manage deployments → New version → Deploy (חובה — `/exec` מגיש את הגרסה הפרוסה).
4. *(מומלץ)* הרץ **`runImport`** פעם אחת — ינרמל את שמות-התוכנית של הרשומות הקיימות לתוויות הנקיות (העדכון מתבצע גם בלי זה, כי ה-UI מנרמל בתצוגה).
5. בדיקת קבלה של טל על שלוש התוכניות.

### לא מומש בכוונה (לאישור)
- **דריסת-שלב ידנית** — לא נדרשה: המודל מבוסס-אבני-דרך כבר גמיש (אפשר לסמן/לבטל כל שלב, לדלג, לחזור), והשלב נגזר מעצמו. אם טל עדיין תרצה דריסה ידנית — נוסיף שדה.
- **קליטת Excel מג'אפא (Phase E)** — נדחתה לסוף לפי החלטת נועם; צריך דגימת קובץ-ייצוא אמיתי.

---

## עדכון מצב — 2026-06-22 (Cutover: האפליקציה הופכת ל-master)

החלטת נועם: **האפליקציה היא מקור-האמת (master), לא משקפת.** טל עוברת לעבוד דרכה בלבד; הקובץ הישן הופך לארכיב. טכנית האפליקציה כבר קוראת/כותבת ל-App DB משלה — השינוי הוא בהגנות שמבטיחות שלא תאבד נתונים ולא תידרס ע"י ייבוא.

### מה השתנה
- **Importer בטוח (insert-only):** `runImport()` (וגם כפתור "⟳ משיכת חדשים מהארכיב") מוסיף **רק רשומות חדשות** ולעולם לא דורס עריכות שטל עשתה באפליקציה. `runFullResync()` (מהעורך בלבד) = ה-upsert המלא הישן, לסנכרון סופי חד-פעמי לפני המעבר.
- **`APP_DB_ID` → Script Properties:** `getAppDbId_()` קורא קודם מ-Script Properties, ו-`setup()` שומר שם. כך הדבקת `config.gs` מחדש **לא תאבד את המצביע למסד-האב** (החוב הטכני שתויק — נסגר). חד-פעמי: הרצת `saveAppDbIdToProperties()` מעתיקה את ה-id הנוכחי מ-config.
- **ולידציה ביצירה:** `apiCreate` דורש לפחות שם או טלפון (לא יוצרים רשומות ריקות במסד-האב).
- **`normalizeExistingPrograms()`** — חד-פעמי, מנרמל שמות-תוכנית קיימים לתוויות נקיות בלי למשוך מהקובץ הישן.

### רצף ה-Cutover (פעם אחת, תחת חשבון המכון)
1. הדבק את כל קבצי ה-.gs המעודכנים + Redeploy.
2. הרץ `saveAppDbIdToProperties()` פעם אחת (מקבע את מזהה מסד-האב).
3. *(אופציונלי, מומלץ)* הרץ `runFullResync()` **פעם אחת** — משיכה סופית של הקובץ הישן (כולל נרמול תוכניות). מכאן והלאה **לא** מריצים resync שוב.
4. מודיעים לטל: מהיום עובדים **רק באפליקציה**. הקובץ הישן = ארכיב לקריאה.
5. מכאן: פניות חדשות נוצרות באפליקציה (+ "פנייה חדשה" / עתידית טופס). כפתור "משיכת חדשים" נשאר כרשת-ביטחון insert-only.

> **הערה:** מאחר שה-importer עכשיו insert-only, אם לא תריץ `runFullResync` בשלב 3 — נתונים ישנים שכבר במסד יישארו כפי שהם (לא ידרסו), ורק רשומות-קובץ חדשות יתווספו. זו ההתנהגות הרצויה ל-master.

---

## עדכון מצב — 2026-06-24 (קליטה ממייל + קארדקום + קורסים + תיקוני UX)

קבצים חדשים: `email_ingest.gs` (קליטת פניות Amax), `cardcom_ingest.gs` (סימון תשלומים + קורסים). git: commit `8bbed29`.

### קליטת פניות ממייל (אוטומטי, בענן)
- מיילי Amax "הודעה על רישום" מועברים ל-`merkazrotenbergkh` → טריגר שעתי קולט → יוצר פנייה. **התוכנית מזוהה מכותרת המייל** (`programFromSubject_`: "פסיכותרפיה"→הדיאלוגי). HTML-fallback. דה-דופ לפי קוד/טלפון.
- ⚠️ באג שתוקן: **כוכביות** (bold של Gmail בהעברה) שברו את ה-regex → רשומות לא נוצרו. `parseRegistrations_` מסיר כוכביות. `reingestRegistrations` חד-פעמי לתיקון.

### סימון תשלומים מקארדקום (שלד, טריגר מותקן)
- חשבוניות `purchase@out.cardcom.co.il` מועברות (פילטר Gmail מ-noamlow) → `cardcom_ingest` מפענח (סכום/תיאור/משלם/מס' עסקה), מתאים לפנייה (מייל→טלפון→שם), ומסמן את אבן-הדרך לפי **תיאור המוצר** (רק דמי-מסלול; קורסים/ספרים מדולגים). דה-דופ לפי מס' עסקה.

### קורסים — רישום עצמאי + התראת מודל
- רכישת **קורס רבי נחמן** (`COURSE_PRODUCTS`) → `recordType='קורס'` + `needsMoodle=true`. התראה מהבהבת בראש "📌 N קורסים להכניס למודל" + לשונית "קורסים" + כפתור "✓ הוכנס למודל". מוחרגים מהמשפך.

### תיקוני UX (משוב טל)
- **סימון עובדה מזיז את הכרטיס:** `apiUpdate` מנקה את `boardStage` הידני כששדה עובדה משתנה → המשפך המחושב חוזר לשלוט (תיקון "לא מגיב").
- **אזהרת שער מדויקת:** תווית הלוח הייתה קבועה "⚠ דמי הרשמה" גם כשהחוסר במסמכים → עכשיו "⚠ חסר לריאיון" + פירוט בריחוף.
- **בקרת שלב בכרטיס:** בורר "שלב במשפך" — שינוי ידני כמו גרירה, עם אזהרת-דילוג.

### OAuth קריאה (לבדיקות בלבד, נפרד מהצינור האוטומטי)
- `merkazrotenberg.noam` + `merkazrotenbergkh` → `~/.config/gws/credentials_*.json` (gmail.readonly). שניהם test-users ב-GCP `claude-workspace-489320`. הרצה עם `NODE_OPTIONS=--use-system-ca`.

### לוח-זמנים לקליטה (בקשת טל, commit `e4ad2f7`)
`scheduledIngest()` מריץ את **שני** ה-ingesters (פניות + תשלומים) **כל 10 דק' ב-08:00–12:00 וב-18:00–20:30** (שעות העומס), אחרת שעתי. `installSchedule()` מחליף את הטריגרים השעתיים הקודמים בטריגר אחד `everyMinutes(10)` עם gate לפי שעה. (תיקן מצב שבו פניות נתקעו כי הטריגר השעתי לא רץ.)

### אבטחה — אין נתוני אשראי/ת.ז. במאגר
`cardcom_ingest` מחלץ אך ורק: שם / מייל / טלפון / סכום / תיאור-מוצר / מס'-עסקה. **לא** מחלץ ולא שומר: מספר כרטיס, 4 ספרות אחרונות, ת.ז., סוג כרטיס. גוף המייל הגולמי לא נשמר. (קארדקום ממילא לא שולחת מספר כרטיס מלא — רק 4 ספרות, שגם אותן איננו נוגעים.)

### ⏳ פתוח
- לאמת forward-קארדקום בתשלום הבא (אין דליפה — מאומת); כיול סכומי קארדקום; ניקוי כפילות ישנה; `runFullResync`.

---

## עדכון מצב — 2026-07-12 (הלוח כמשטח-עבודה: בחירה מרובה + פילוח-זמן)

בקשת טל: *"בשלב הזה אני מעדיפה להסתכל בפורמט של לוח כי יש לי זכרון חזותי."* + *"כל האנשים שהם בשלב של התעניינות וצריך לחזור אליהם שוב — שיהיה אפשר לסמן את כולם יחד ולהעתיק את מספרי הטלפון לרשימה אחת להדבקה בוואטסאפ (ולא רק אחד-אחד)."* + פילוח המתעניינים לפי זמן כניסה. **שינוי ב-`index.html` בלבד** — לוגיקת השרת/הסכמה לא נגעה.

### מה נוסף (בלשונית "לוח")
1. **בחירה מרובה + העתקת טלפונים (05X):** כפתור **"✔ בחירה מרובה"** בסרגל מדליק מצב-בחירה — צ'ק-בוקס על כל כרטיס + צ'ק-בוקס **"בחר עמודה שלמה"** בכותרת כל שלב. סרגל צף בתחתית: **📋 העתק טלפונים** מעתיק את כל הנבחרים כרשימה אחת, מספר בשורה, בפורמט ישראלי `05X` (מנרמל 972/קידומות, מסיר כפילויות, מדלג על מי שאין לו טלפון). במצב-בחירה הגרירה מושבתת (לחיצה = סימון).
2. **פילוח לפי זמן כניסה — תת-כותרות בעמודת המתעניינים:** בשלבים המוקדמים (0/1, למעט "נרשם/התקבל") הכרטיסים מקובצים לפי `stageSince` ל-🔴 מעל חודש · 🟠 2–4 שבועות · 🟡 שבוע–שבועיים · 🟢 השבוע (מהוותיק לטרי — המתקררים למעלה).
3. **תצורה מקבילה / "האפשרות השנייה":** הבחירה אינה מקובעת למתעניינים — היא עובדת על **כל חתך** שטל סיננה אליו (תוכנית / שלב / "רק שצריך תזכורת"). זרימת-עבודה: סינון "רק שצריך תזכורת" → "בחר עמודה שלמה" בעמודת ההתעניינות → 📋 → הדבקה בוואטסאפ.
4. **בחירה מרובה גם בטבלה הרוחבית** (משוב נועם, אותו יום): עמודת צ'ק-בוקס בכל שורה + "בחר הכל" בכותרת (מכבד את סינון הטבלה: תוכנית / "צריך טיפול בלבד"). אותו פס-צף התחתון של 📋 העתק טלפונים משותף ללוח ולטבלה (`updateSelBar` מוצג בכל n>0). פונקציות: `toggleSelTable`/`selectAllTable`/`syncTableHead`/`endSelection`. כך אפשר לבחור-מרובה בשתי התצוגות.

### קבצים שהשתנו
`index.html` בלבד — CSS (`.card.sel`/`.selchk`/`.agehdr`/`#selbar`), כפתור `#selBtn` בסרגל, סרגל צף `#selbar`, מבנה `render()` (חילוץ `makeCard`, תת-קבוצות-גיל, מצב-בחירה) + פונקציות `toggleSelMode`/`toggleSel`/`selectCol`/`syncColHeaders`/`copySelectedPhones`/`phone05_`. נבדק: תחביר (`node --check`) + יחידות (נרמול טלפון, זיהוי שלב-התעניינות).

### 🔴 חובה לפריסה
`index.html` מוגש מהגרסה הפרוסה של ה-Web App — **Redeploy: Manage deployments → New version → Deploy** תחת `merkazrotenbergkh@gmail.com`. הדבקה + Ctrl+S לבדם לא מספיקים.

---

## עדכון מצב — 2026-07-12 (סבב הערות טל #4: מילוי-אוטומטי של תאריכי אבני-דרך)

בקשת טל: *"יעזור אם בכרטיס של כל מועמד ימלא את התאריך אוטומטית לפי התאריך שמסמנים את המשבצת לידו, וכמובן אפשרות ידנית לשינוי."*

### מה נוסף
- **`AUTO_DATE_PAIRS`** (`config.gs`) — מקור-אמת יחיד לזוגות ✓↔תאריך. אלו **כל** הזוגות שקיימים בסכמה:
  | משבצת סימון | תאריך צמוד |
  |---|---|
  | `materialSent` (נוצר קשר ונשלח חומר) | `materialDate` |
  | `payReg` (דמי הרשמה שולם) | `payRegDate` |
  | `payDeposit` (מקדמה שולם) | `payDepositDate` |
- **`applyAutoDates_(o)`** (`lib.gs`) — ✓ מסומן **והתאריך הצמוד ריק** → תאריך היום. **לעולם לא דורס תאריך קיים** (ידני או קודם). ביטול סימון **לא** מוחק את התאריך (כדי לא לאבד נתון בטעות).
- **בשרת** (`Code.gs`): נקרא ב-`apiUpdate` וב-`apiCreate` → מכסה גם **גרירה בין שלבים** (ש-`STAGE_APPLY` מסמן בה אבני-דרך), לא רק סימון ידני בכרטיס.
- **בלקוח** (`index.html`): `autoFillDate()` — סימון ה-✓ בכרטיס ממלא את שדה התאריך **מיד** (משוב ויזואלי, רקע תכלת + toast "אפשר לשנות"). השדה נשאר ניתן לעריכה. אם השדה מוסתר בתוכנית (`PROGRAM_CARD_HIDE`) — השרת ממלא בכל מקרה.

### הערה שנייה של טל (תאריכים ללא ✓)
*"יש עוד מקומות של תאריכים שאין לידם משבצת סימון... אבל הוא סופר את הימים שלהם ככה שזה לא קריטי."* — נכון: מוני ההתיישנות מבוססים על `stageSince`/`inquiryDate` שמתמלאים אוטומטית, ולא על התאריכים הידניים. **`interviewDate` הושאר ידני בכוונה** — הוא תאריך *עתידי* (מועד הריאיון שנקבע), ומילוי אוטומטי של "היום" היה שגוי.

### קבצים שהשתנו
`config.gs` (AUTO_DATE_PAIRS) · `lib.gs` (applyAutoDates_) · `Code.gs` (apiUpdate/apiCreate/apiBoard) · `index.html` (autoFillDate + onchange בצ'ק-בוקס).
נבדק: תחביר בכל 4 הקבצים + 6 בדיקות-יחידה ל-`applyAutoDates_` (תאריך ידני נשמר · לא-מסומן נשאר ריק · "TRUE"/סכום מספרי מהגיליון = שולם · אובייקט Date לא נדרס).

### 🔴 פריסה — הפעם **4 קבצים**
להדביק `config.gs` + `lib.gs` + `Code.gs` + `index.html` → Ctrl+S → **Manage deployments → New version → Deploy**. ⚠️ **אין להריץ `setup()`** (מוחק נתוני פניות) — אין צורך, אין עמודות חדשות.
